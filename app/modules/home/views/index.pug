//- This is how we extend a template using the 'extends' keyword
extends ../../../templates/master.pug

//- This is how we insert dynamic content to extended templates
block content
    -  var hasNoAdmin = typeof reqQuery.notadmin !=='undefined' ?'You\'re not an admin':''
    - console.log( typeof reqQuery.notadmin)
    if hasNoAdmin
        section.hero.is-dark
            .hero-body
                .notification.is-danger
                    | #{hasNoAdmin} 
    h1.title Welcome to ExpressJS!
    p If you see a list of users below, then that means you have successfully queried data from the database.
    hr
    h3.title Categories
    ul
        //- This is how we iterate through arrays and access
        //- their elements
        each category in postcategory
            br
            a(href=`/categories/${category.postcategoryid}`)
                li= `${category.postcategoryid} - ${category.categoryname}` 